import React from 'react';


const ProjectSquare = ({imageUrl, name, loc}) => {

  return (
    <div>
      
    </div>
      // <div className='flex flex-col justify-end w-3/4 aspect-square bg-cover bg-center'
      //   style= {{ backgroundImage: `url(${imageUrl})` }}>
      //   <div className='flex h-1/4 text-center justify-center items-center font-display text-white bg-dknavy bg-opacity-70 pt-2 pb-2'>
      //     <a href={`/${name.toLowerCase().replace(/ /g, '-')}`}>
      //       <span className='font-semi text-lg'>{name} </span><br />
      //       <span className='italic text-sm'>{loc}</span>
      //       </a>
      //   </div>
      // </div>
  )
}

export default ProjectSquare